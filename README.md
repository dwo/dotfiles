Sometimes I work on different computers, but I want things to be configured the same way.

## Install

Run `./install` from the repository root.

The install script copies the public coding-agent preferences from `AGENTS.md` to `~/.config/AGENTS.md`.
It recreates `~/.claude/CLAUDE.md` and `~/.codex/AGENTS.md` as symlinks to that shared file.
